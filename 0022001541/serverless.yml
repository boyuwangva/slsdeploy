service: jenkins-test-2 # NOTE: update this with your service name


provider:
  name: aws
  runtime: python2.7

  region: ap-northeast-2

functions:
  vam:
    handler: handler.hello
    name: jenkins-test-1
    events:
      - schedule:
          rate: cron(0 0 ? * FRI *)
          enabled: true
    
