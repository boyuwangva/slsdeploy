service: jenkins-test-30 # NOTE: update this with your service name


provider:
  name: aws
  runtime: python2.7

  region: ap-northeast-2

functions:
  vam:
    handler: handler.hello
    name: jenkins-test-30
    events:
      - schedule:
          rate: cron(0 0 ? * FRI *)
          enabled: true
    
